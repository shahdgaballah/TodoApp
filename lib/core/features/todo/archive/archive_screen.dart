import 'package:flutter/cupertino.dart';

class  ArchiveScreen extends StatelessWidget {
  const  ArchiveScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('test archive'),
    );
  }
}
